// Re-export HeaderDynamic as Header for backward compatibility
// This allows all existing imports to continue working while using the dynamic version
import HeaderDynamic from './HeaderDynamic';

export default HeaderDynamic;
